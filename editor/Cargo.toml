
[package]
name = "editor"
version = "0.1.0"
edition = "2021"

[dependencies]
fyrox = { workspace = true }
fyroxed_base = { workspace = true }
first_proj = { path = "../game", optional = true }

[features]
default = ["first_proj", "fyroxed_base/default"]
dylib = ["fyroxed_base/dylib_engine"]
