
[package]
name = "executor"
version = "0.1.0"
edition = "2021"

[dependencies]
fyrox = { workspace = true }
first_proj = { path = "../game", optional = true }

[features]
default = ["first_proj"]
dylib = ["fyrox/dylib"]
